using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ObjectPooling
{
    /// <summary>
    /// This is just a demo class which is used to spawn and release objects in the pool.
    /// It doesn't hold any functionality but serves as a placeholder for pooling.
    /// </summary>
    public class PoolObject : MonoBehaviour
    {
    }
}
